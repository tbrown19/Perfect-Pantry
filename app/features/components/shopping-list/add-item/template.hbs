{{#paper-card as |card|}}
	{{#paper-toolbar class = "toolbar"}}
		{{#paper-toolbar-tools}}
			<h2>{{fa-icon 'plus'}}&nbsp;Add item</h2>
			<span class="flex"></span>
			<button class="collapse-button" data-toggle="collapse" data-target="#addItemCardContent" {{action 'collapseButton'}}>{{paper-icon currentIcon}}</button>
		{{/paper-toolbar-tools}}
	{{/paper-toolbar}}

	<div id="addItemCardContent" class="collapse in">
		{{#card.content}}
			{{#if requestingItemName}}
				{{shopping-list/add-item/get-item-name itemName=itemName getItemName="getItemName"}}
			{{else if requestingItemQty}}
				{{shopping-list/add-item/get-item-qty getItemQty="getItemQty" goBack="goBack"}}
			{{else}}

			{{/if}}
		{{/card.content}}
	</div>
{{/paper-card}}
